#!yasl

{ = "0" >= 2 ^ "9" <= &&
} ft_isdigit

3 < ? ( " <start_num> <value1> [ <value2> [...] ]\n" + print 1 exit )

# !^ // Stash program name
= "" == ? ( # ^ ": <start_num> needs to be strictly positive integer\n" + print 1 exit)
= ][:2
2:=:3
2:=
2:!
#(2:= 2:!)
3:=
3:!
2 ^ "+" %== ? ( ! ! 1 -) : ( ! 2 ^ )
#(
    ft_isdigit ~~ ? (
        ( # ^ ": <start_num> needs to be strictly positive integer\n" + print 1 exit)
    )
)
// Cast <start_num> from string to integer
0 +
= 0 == ? ( # ^ ": <start_num> needs to be strictly positive integer\n" + print 1 exit)
// Program name no longer needed, get rid of it
# ^ !

# 1 - #( // Repeat for each value
    = 3 !^ #( = print )
    !
    "\n" print
    1 +
)
